<h1>Booking</h1>

<p><% @booking.each do |l| %>
<%= l.user_game_id %>
<%= l.user_id %>
<%= l.status %>
<%= l.start_date %>
<%= l.end_date %>
<%= l.user_id %>
<% end %>

<button class="btn btn-primary">  <%= link_to "All bookings", user_booking_index_path(:id) %>
